/*
	Package command encapsulates routines to process string commands and their
	optional settings.  Since these routines are used at multiple DVID layers,
	we separate them here and allow the core functions to be reused through embedding.
*/
package command
