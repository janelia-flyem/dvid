/*
Package terminal provides a stateful client for DVID interaction.  Unlike using 
DVID commands from the shell, terminal use keeps several DVID values 
(e.g., server addresses, image version UUID) in memory and provides them
automatically to the DVID server.
*/
package terminal
