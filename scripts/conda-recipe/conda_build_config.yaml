#
# This is a conda build config file.
#
# For details on these settings, see:
# https://docs.conda.io/projects/conda-build/en/latest/resources/variants.html
#


# CONDA_BUILD_SYSROOT:
#   # Set this when pinning release builds to a specific macOS SDK supported
#   # by the Go version in go.mod and the conda-forge compiler stack.
#   - /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk

# MACOSX_DEPLOYMENT_TARGET:
#   # Set this when release builds need to target an older macOS version.
#   - 11.0
